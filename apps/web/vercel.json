{
    "version": 2,
    "buildCommand": "pnpm build:web",
    "outputDirectory": "apps/web/.next",
    "framework": "nextjs",
    "installCommand": "pnpm install --filter=@free2talk/web --filter=@free2talk/shared"
}