{
    "version": 2,
    "buildCommand": "cd ../.. && pnpm install --filter=@free2talk/web --filter=@free2talk/shared --frozen-lockfile && pnpm build:web",
    "outputDirectory": "apps/web/.next",
    "framework": "nextjs",
    "installCommand": "echo 'Skipping auto-install'"
}